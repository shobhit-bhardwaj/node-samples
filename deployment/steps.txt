1. Add helmet and compression in Express Middleware.
 
2. Add Following Step in package.json

  "scripts": {
    "test": "jest --verbose --coverage",
    "start": "node XXX.js"
  },

  "engines": {
  	"node": "8.9.1"
  }


3. Create a GIT Repository
	git init
	git add .
	git commit -m "Initial Commit"